# Models Folder

Place OBJ model files exported from Blender here.

When exporting from Blender:
- File → Export → Wavefront (.obj)
- Enable: Include Normals, Include UVs, Apply Modifiers
- Export each major object separately (bed.obj, desk.obj, etc.)

