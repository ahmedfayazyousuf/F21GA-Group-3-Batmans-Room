# Textures Folder

Place texture image files here (PNG, JPG format).

This folder is for texture images used by materials. Currently the material system supports base colors, but texture loading can be added to the Material class if needed.

